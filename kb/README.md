# Knowledge Base

The `kb/` directory is the primary source for all session builds. Pages are Karpathy-style —
ground-up explanations with code, not prose summaries. Claude reads these before building; when
a page is missing for something needed, the gap is flagged inline and logged in `GAPS.md`.
